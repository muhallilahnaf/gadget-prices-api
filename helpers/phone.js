const phoneReqValidation = (body) => {

    const keys = ['shop', 'name', 'link', 'price', 'brand', 'status']

    let valid = true

    // keys.some(key => {
    //     if (!body[key]) 
    // })
}

module.exports = {
    phoneReqValidation
}